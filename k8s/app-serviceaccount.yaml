# The identity the workload runs as. Vault's Kubernetes auth role is bound to
# THIS service account (in THIS namespace), so only pods running as app-sa in
# the demo namespace can authenticate and read the secret.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: app-sa
  namespace: demo
